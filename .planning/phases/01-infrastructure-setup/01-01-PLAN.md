---
phase: 01-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/main.py",
  "backend/database.py",
  "backend/routers/webhook.py",
  "backend/routers/incidents.py",
  "backend/routers/reports.py",
  "backend/services/ai_dispatcher.py",
  "backend/services/twilio_service.py",
  "backend/services/pdf_generator.py",
  "backend/models/schemas.py",
  "backend/requirements.txt",
  "rakshak-dashboard/package.json",
  ".env",
  ".env.local"
]
autonomous: true
requirements: []
user_setup:
  - service: supabase
    why: "Need PostgreSQL + PostGIS, Realtime, and Storage for MVP"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Project Settings"
      - name: SUPABASE_SERVICE_KEY
        source: "Supabase API Settings"
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase API Settings"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase API Settings"
    dashboard_config:
      - task: "Run SQL Schema"
        location: "Supabase SQL Editor"
        details: "Run the provided schema for users, locations, and incidents"
  - service: twilio
    why: "WhatsApp Business API + WebRTC requires account credentials"
    env_vars:
      - name: TWILIO_ACCOUNT_SID
        source: "Twilio Console"
      - name: TWILIO_AUTH_TOKEN
        source: "Twilio Console"
      - name: TWILIO_WHATSAPP_FROM
        source: "Twilio Sandbox"
  - service: openai
    why: "GPT-4o and Whisper-1 require API key"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform"
  - service: mapbox
    why: "3D map requires token"
    env_vars:
      - name: NEXT_PUBLIC_MAPBOX_TOKEN
        source: "Mapbox Account"
must_haves:
  truths:
    - "FastAPI backend has required directories and entrypoint"
    - "Next.js frontend application is initialized"
  artifacts:
    - path: "backend/main.py"
      provides: "FastAPI application entrypoint"
    - path: "rakshak-dashboard/package.json"
      provides: "Next.js frontend configuration"
  key_links: []
---

<objective>
Set up Supabase, FastAPI, Next.js, and Twilio webhooks

Purpose: Initialize the project infrastructure so that subsequent AI logic and Dashboard UI can be built on top of a solid, connected skeleton.
Output: Initialized backend directory with FastAPI placeholder files, frontend Next.js scaffold in `rakshak-dashboard`, and environment variables templates.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold FastAPI Backend</name>
  <files>backend/main.py, backend/routers/webhook.py, backend/routers/incidents.py, backend/routers/reports.py, backend/services/ai_dispatcher.py, backend/services/twilio_service.py, backend/services/pdf_generator.py, backend/models/schemas.py, backend/database.py, backend/requirements.txt, .env</files>
  <read_first></read_first>
  <action>
    Create the backend directory structure. Create a basic FastAPI entrypoint in backend/main.py. Set up empty placeholder files for routers, services, and models. 
    Create backend/requirements.txt with required packages: fastapi==0.111.0, uvicorn==0.29.0, openai==1.30.0, twilio==9.1.0, reportlab==4.2.0, supabase==2.4.0, sqlalchemy==2.0.0, asyncpg==0.29.0, python-dotenv==1.0.0, pydantic==2.7.0, python-multipart==0.0.9.
    Create a .env file with placeholder variables.
  </action>
  <verify>ls backend/main.py</verify>
  <acceptance_criteria>
    - backend/main.py exists
    - backend/requirements.txt contains specified packages
    - backend/services and backend/routers directories exist
  </acceptance_criteria>
  <done>Backend skeleton is scaffolded</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js Frontend</name>
  <files>rakshak-dashboard/package.json, .env.local</files>
  <read_first></read_first>
  <action>
    Run `npx -y create-next-app@14 rakshak-dashboard --typescript --tailwind --app --use-npm` to scaffold the frontend application. (Run in non-interactive mode).
    After scaffolding, run `npm install mapbox-gl@3.0.0 react-map-gl@7.1.0 @supabase/supabase-js@2.43.0 lucide-react@0.378.0 zustand@4.5.0 axios@1.7.0 react-pdf@7.7.0` inside the `rakshak-dashboard` directory.
    Create .env.local with placeholder variables inside `rakshak-dashboard`.
  </action>
  <verify>ls rakshak-dashboard/package.json</verify>
  <acceptance_criteria>
    - rakshak-dashboard directory exists and has a Next.js app
    - rakshak-dashboard/package.json has mapbox-gl and supabase-js dependencies
  </acceptance_criteria>
  <done>Frontend skeleton is scaffolded</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cat backend/requirements.txt` contains required dependencies
- [ ] `cat rakshak-dashboard/package.json` contains required dependencies
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-setup/01-01-SUMMARY.md`
</output>
