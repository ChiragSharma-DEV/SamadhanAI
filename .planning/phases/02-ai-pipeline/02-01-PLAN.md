---
phase: 02-ai-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/database.py",
  "backend/models/schemas.py",
  "backend/services/ai_dispatcher.py",
  "backend/services/twilio_service.py",
  "backend/routers/webhook.py",
  "backend/main.py"
]
autonomous: true
requirements: ["CORE-01", "CORE-02", "AI-01"]
user_setup: []
must_haves:
  truths:
    - "System can receive WhatsApp incoming messages"
    - "System can transcribe audio to text using Whisper"
    - "System can extract structured data using GPT-4o"
    - "Incidents are stored in Supabase"
    - "A confirmation reply is sent via Twilio"
  artifacts:
    - path: "backend/routers/webhook.py"
      provides: "Twilio webhook endpoint"
    - path: "backend/services/ai_dispatcher.py"
      provides: "Whisper and GPT-4o integration"
    - path: "backend/services/twilio_service.py"
      provides: "WhatsApp reply sender"
  key_links:
    - from: "backend/routers/webhook.py"
      to: "backend/services/ai_dispatcher.py"
      via: "Calls AI processing"
    - from: "backend/routers/webhook.py"
      to: "backend/database.py"
      via: "Calls Supabase insert"
---

<objective>
Implement Twilio webhooks, Whisper, GPT-4o, and DB insertion logic.

Purpose: To create the core backend logic that turns raw citizen distress signals (WhatsApp audio/text) into structured incident records in the database, closing the AI triage loop.
Output: Working FastAPI routes and service modules for Twilio, OpenAI, and Supabase integrations.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Database Client and Schemas</name>
  <files>backend/database.py, backend/models/schemas.py</files>
  <read_first></read_first>
  <action>
    In `backend/database.py`, initialize the Supabase client using `SUPABASE_URL` and `SUPABASE_SERVICE_KEY` environment variables.
    In `backend/models/schemas.py`, define Pydantic models for the incoming webhook payload, the extracted incident data (intent, severity, location), and the database insert schema. Ensure severity is an Enum (Critical, High, Medium, Low).
  </action>
  <verify>python -c "import backend.models.schemas"</verify>
  <acceptance_criteria>
    - `backend/database.py` exports a configured Supabase client.
    - `backend/models/schemas.py` defines Incident extracted data schema.
  </acceptance_criteria>
  <done>Database client and data models are established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI Dispatcher</name>
  <files>backend/services/ai_dispatcher.py</files>
  <read_first>backend/models/schemas.py</read_first>
  <action>
    Implement `transcribe_audio(audio_url: str)` that downloads the audio from Twilio and sends it to `openai.audio.transcriptions` (Whisper-1).
    Implement `extract_incident_details(text: str)` that uses `openai.chat.completions` (GPT-4o) with structured output to extract `intent` (what happened), `severity`, and `location` (string or coordinates) from the text.
  </action>
  <verify>python -c "from backend.services.ai_dispatcher import transcribe_audio, extract_incident_details"</verify>
  <acceptance_criteria>
    - `backend/services/ai_dispatcher.py` contains working logic calling OpenAI APIs.
  </acceptance_criteria>
  <done>AI logic for Whisper and GPT-4o is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Twilio Service</name>
  <files>backend/services/twilio_service.py</files>
  <read_first></read_first>
  <action>
    Initialize the Twilio Client using `TWILIO_ACCOUNT_SID` and `TWILIO_AUTH_TOKEN`.
    Implement `send_whatsapp_message(to: str, body: str)` that sends a message from `TWILIO_WHATSAPP_FROM` to the provided number.
  </action>
  <verify>python -c "from backend.services.twilio_service import send_whatsapp_message"</verify>
  <acceptance_criteria>
    - `backend/services/twilio_service.py` successfully initializes and exports send message function.
  </acceptance_criteria>
  <done>Twilio service logic is implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Webhook Router</name>
  <files>backend/routers/webhook.py, backend/main.py</files>
  <read_first>backend/models/schemas.py, backend/services/ai_dispatcher.py, backend/services/twilio_service.py, backend/database.py</read_first>
  <action>
    In `backend/routers/webhook.py`, create a POST endpoint `/api/webhook/twilio` that accepts Twilio's form-encoded payload.
    Extract the sender's number and message body (or audio URL if MediaUrl0 is present).
    If audio, pass to `transcribe_audio`. If text, pass directly to `extract_incident_details`.
    Insert the resulting incident data into the `incidents` table in Supabase.
    Generate a tracking link (e.g., `https://rakshak.ai/stream/{incident_id}`).
    Send a confirmation back to the sender via `send_whatsapp_message` containing the summary and the tracking link.
    In `backend/main.py`, include the router (`app.include_router(webhook.router)`).
  </action>
  <verify>python -c "from backend.main import app"</verify>
  <acceptance_criteria>
    - Webhook route parses incoming data, triggers AI functions, stores to DB, and replies.
    - Router is included in the main FastAPI application.
  </acceptance_criteria>
  <done>End-to-end webhook route is fully wired up.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -m py_compile backend/**/*.py` to check for syntax errors.
- [ ] Confirm `backend/main.py` correctly imports the router.
</verification>

<success_criteria>
- All tasks completed successfully.
- Webhook logic handles both text and audio inputs gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-pipeline/02-01-SUMMARY.md`
</output>
