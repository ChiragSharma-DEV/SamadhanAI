---
phase: 03-admin-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "backend/services/pdf_generator.py",
  "backend/routers/reports.py",
  "backend/main.py",
  "rakshak-dashboard/src/components/IncidentDetail.tsx",
  "rakshak-dashboard/src/components/WebRTCStream.tsx",
  "rakshak-dashboard/src/app/page.tsx"
]
autonomous: true
requirements: ["UI-01", "DOC-01"]
user_setup: []
must_haves:
  truths:
    - "Clicking an incident shows details"
    - "A PDF FIR report can be generated and downloaded"
  artifacts:
    - path: "backend/services/pdf_generator.py"
      provides: "ReportLab PDF generation"
    - path: "backend/routers/reports.py"
      provides: "PDF download endpoint"
    - path: "rakshak-dashboard/src/components/IncidentDetail.tsx"
      provides: "Detailed incident view with report generation button"
  key_links:
    - from: "rakshak-dashboard/src/components/IncidentDetail.tsx"
      to: "backend/routers/reports.py"
      via: "PDF download link"
---

<objective>
Implement incident detail drawer, WebRTC UI, and ReportLab PDF endpoint.

Purpose: Provide deep dive capability for an incident, including live video feed simulation and generation of a legal FIR PDF report.
Output: FastAPI PDF generation endpoint and Next.js detailed incident view component.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-dashboard/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend PDF Generator Service</name>
  <files>backend/services/pdf_generator.py</files>
  <read_first></read_first>
  <action>
    Implement `generate_fir_pdf(incident: dict) -> bytes` using `reportlab`. Design a structured FIR report showing Incident ID, Timestamp, Intent, Severity, Location, and Raw Text. Return the PDF bytes.
  </action>
  <verify>python -c "from backend.services.pdf_generator import generate_fir_pdf"</verify>
  <acceptance_criteria>
    - `generate_fir_pdf` successfully uses ReportLab to create a PDF.
  </acceptance_criteria>
  <done>PDF generation logic is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Backend Reports Router</name>
  <files>backend/routers/reports.py, backend/main.py</files>
  <read_first>backend/services/pdf_generator.py, backend/database.py</read_first>
  <action>
    Create a new GET endpoint `/api/reports/fir/{incident_id}` in `backend/routers/reports.py`. Fetch the incident from Supabase. Generate the PDF via `generate_fir_pdf` and return it as a `Response` with `media_type="application/pdf"` and `Content-Disposition` header for download. Include this router in `backend/main.py`.
  </action>
  <verify>cat backend/routers/reports.py</verify>
  <acceptance_criteria>
    - Endpoint fetches data and returns a downloadable PDF.
  </acceptance_criteria>
  <done>PDF download endpoint is active.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend WebRTC Placeholder</name>
  <files>rakshak-dashboard/src/components/WebRTCStream.tsx</files>
  <read_first></read_first>
  <action>
    Create a UI component `WebRTCStream` that simulates a live video feed (for the MVP hackathon scope, this can be a styled box with a "Live Feed Connecting..." or a mock video element).
  </action>
  <verify>ls rakshak-dashboard/src/components/WebRTCStream.tsx</verify>
  <acceptance_criteria>
    - Component exists and renders correctly.
  </acceptance_criteria>
  <done>WebRTC UI is created.</done>
</task>

<task type="auto">
  <name>Task 4: Frontend Incident Detail Drawer</name>
  <files>rakshak-dashboard/src/components/IncidentDetail.tsx, rakshak-dashboard/src/app/page.tsx, rakshak-dashboard/src/store/incidentStore.ts</files>
  <read_first>rakshak-dashboard/src/components/WebRTCStream.tsx</read_first>
  <action>
    Update Zustand store with `selectedIncidentId` state. Create `IncidentDetail.tsx` that appears as a drawer or overlay when an incident is selected.
    Include the `WebRTCStream` component inside it.
    Add a "Download FIR Report" button that creates an anchor tag linking to the FastAPI `GET /api/reports/fir/{incident_id}` endpoint.
    Update `page.tsx` or `IncidentList.tsx` to set the selected incident on click, rendering the `IncidentDetail` component.
  </action>
  <verify>ls rakshak-dashboard/src/components/IncidentDetail.tsx</verify>
  <acceptance_criteria>
    - Detail view shows selected incident data, WebRTC placeholder, and working download button.
  </acceptance_criteria>
  <done>Detail view and PDF download are wired up.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Check Next.js build compilation.
</verification>

<success_criteria>
- All tasks completed successfully.
- The UI handles selecting incidents and provides PDF download capability.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-dashboard/03-02-SUMMARY.md`
</output>
