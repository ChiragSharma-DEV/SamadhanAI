---
phase: 03-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "rakshak-dashboard/src/app/page.tsx",
  "rakshak-dashboard/src/components/Map.tsx",
  "rakshak-dashboard/src/components/IncidentList.tsx",
  "rakshak-dashboard/src/lib/supabase.ts",
  "rakshak-dashboard/src/store/incidentStore.ts"
]
autonomous: true
requirements: ["UI-01"]
user_setup: []
must_haves:
  truths:
    - "Admin dashboard displays a map"
    - "Incidents are listed in the sidebar"
    - "New incidents appear in real-time"
  artifacts:
    - path: "rakshak-dashboard/src/components/Map.tsx"
      provides: "Mapbox 3D map component"
    - path: "rakshak-dashboard/src/store/incidentStore.ts"
      provides: "Zustand state with Supabase Realtime subscription"
  key_links: []
---

<objective>
Build core layout, Mapbox integration, and Supabase Realtime incident list.

Purpose: Provide a live, real-time command center for authorities to see incidents on a 3D map exactly as they come in.
Output: Supabase client, Zustand store, Mapbox component, and sidebar list integrated into the main page.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase and State Management Setup</name>
  <files>rakshak-dashboard/src/lib/supabase.ts, rakshak-dashboard/src/store/incidentStore.ts</files>
  <read_first></read_first>
  <action>
    Create `rakshak-dashboard/src/lib/supabase.ts` and initialize the Supabase JS client using `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
    Create a Zustand store in `rakshak-dashboard/src/store/incidentStore.ts` that fetches initial incidents and sets up a Supabase Realtime subscription to the `incidents` table to automatically update the store on INSERT.
  </action>
  <verify>ls rakshak-dashboard/src/store/incidentStore.ts</verify>
  <acceptance_criteria>
    - Supabase client is correctly initialized.
    - Zustand store handles initial fetch and real-time subscription.
  </acceptance_criteria>
  <done>Frontend data layer with real-time capability is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Mapbox Component</name>
  <files>rakshak-dashboard/src/components/Map.tsx</files>
  <read_first>rakshak-dashboard/src/store/incidentStore.ts</read_first>
  <action>
    Create `rakshak-dashboard/src/components/Map.tsx` using `react-map-gl`. Connect it to the Zustand store to pull incident locations. Render markers for each incident on the map (use red markers for Critical, orange for High, etc.). Ensure `NEXT_PUBLIC_MAPBOX_TOKEN` is used. Use a dark theme map style.
  </action>
  <verify>ls rakshak-dashboard/src/components/Map.tsx</verify>
  <acceptance_criteria>
    - Map component renders Mapbox instance.
    - Incident coordinates are mapped to Markers.
  </acceptance_criteria>
  <done>Map integration is functional.</done>
</task>

<task type="auto">
  <name>Task 3: Incident Sidebar</name>
  <files>rakshak-dashboard/src/components/IncidentList.tsx</files>
  <read_first>rakshak-dashboard/src/store/incidentStore.ts</read_first>
  <action>
    Create `rakshak-dashboard/src/components/IncidentList.tsx`. Connect to Zustand store and render a scrollable list of active incidents, showing intent, severity, and time. Use Tailwind for a dark "Cyber-Sentinel" aesthetic.
  </action>
  <verify>ls rakshak-dashboard/src/components/IncidentList.tsx</verify>
  <acceptance_criteria>
    - IncidentList renders data from Zustand store with correct styling.
  </acceptance_criteria>
  <done>Sidebar list is implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Main Layout</name>
  <files>rakshak-dashboard/src/app/page.tsx</files>
  <read_first>rakshak-dashboard/src/components/Map.tsx, rakshak-dashboard/src/components/IncidentList.tsx</read_first>
  <action>
    Modify `rakshak-dashboard/src/app/page.tsx` to have a full-screen layout. A left sidebar (e.g., w-1/3 or w-96) containing `IncidentList`, and the remaining right area containing `Map`. Apply global dark mode classes.
  </action>
  <verify>cat rakshak-dashboard/src/app/page.tsx</verify>
  <acceptance_criteria>
    - Layout contains both Map and IncidentList components side by side.
  </acceptance_criteria>
  <done>Dashboard layout is assembled.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Check Next.js build compilation (`npm run build` in frontend, ignoring strict type errors if necessary for MVP).
</verification>

<success_criteria>
- All tasks completed successfully.
- Code structurally connects the Map and List to real-time state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-dashboard/03-01-SUMMARY.md`
</output>
