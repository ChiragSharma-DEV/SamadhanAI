---
phase: 04-testing-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/seed.py",
  "rakshak-dashboard/src/app/layout.tsx",
  "rakshak-dashboard/tailwind.config.ts",
  "DEMO.md"
]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Database is seeded with demo incidents"
    - "UI matches Cyber-Sentinel theme"
    - "Demo guide exists"
  artifacts:
    - path: "backend/seed.py"
      provides: "Seed script for pitch demo"
    - path: "DEMO.md"
      provides: "Demo script and running instructions"
  key_links: []
---

<objective>
End-to-end testing, UI polish, demo data seeding.

Purpose: To ensure the MVP is 100% ready for the hackathon pitch, pre-populated with realistic data, visually stunning, and has a clear demo narrative.
Output: A seed script, final UI style tweaks, and a `DEMO.md` file.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Seed Script</name>
  <files>backend/seed.py</files>
  <read_first>backend/database.py</read_first>
  <action>
    Create `backend/seed.py` that connects to Supabase using the existing client in `backend/database.py`.
    It should clear the `incidents` table (if safe) or simply insert 3 realistic mock incidents:
    1. A Critical fire at Connaught Place.
    2. A High severity medical emergency (cardiac arrest) in South Extension.
    3. A Medium severity robbery report in Dwarka.
    This provides instant visual data on the dashboard when the judges see it.
  </action>
  <verify>python -c "import backend.seed"</verify>
  <acceptance_criteria>
    - `backend/seed.py` contains executable code to populate Supabase.
  </acceptance_criteria>
  <done>Seed script is ready.</done>
</task>

<task type="auto">
  <name>Task 2: UI Theme Polish</name>
  <files>rakshak-dashboard/src/app/layout.tsx, rakshak-dashboard/tailwind.config.ts</files>
  <read_first></read_first>
  <action>
    Update `rakshak-dashboard/src/app/layout.tsx` to include a global header or just ensure the body has `bg-black text-white`.
    Add Google Font "Inter" or "Outfit" to make it look premium.
    Update `tailwind.config.ts` if any custom colors are needed for the Cyber-Sentinel theme (e.g., deep space grays, neon accents).
  </action>
  <verify>cat rakshak-dashboard/src/app/layout.tsx</verify>
  <acceptance_criteria>
    - Font is applied.
    - Global dark theme is enforced at the layout level.
  </acceptance_criteria>
  <done>Cyber-Sentinel aesthetic is locked in.</done>
</task>

<task type="auto">
  <name>Task 3: Create DEMO.md</name>
  <files>DEMO.md</files>
  <read_first></read_first>
  <action>
    Create `DEMO.md` in the root directory. Document the exact sequence for the hackathon pitch:
    1. Start the backend (`uvicorn backend.main:app --reload`).
    2. Start the frontend (`npm run dev` in `rakshak-dashboard`).
    3. Run the seed script (`python -m backend.seed`).
    4. Open `localhost:3000` to show the populated dashboard.
    5. Send a live voice note to the Twilio WhatsApp number to demonstrate real-time pipeline.
    6. Click the new incident and download the FIR PDF.
  </action>
  <verify>ls DEMO.md</verify>
  <acceptance_criteria>
    - `DEMO.md` contains clear, step-by-step instructions.
  </acceptance_criteria>
  <done>Demo narrative is documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `DEMO.md` exists.
</verification>

<success_criteria>
- All tasks completed successfully.
- Pitch materials are ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-polish/04-01-SUMMARY.md`
</output>
